# Copy to .env.local and fill in. Never commit .env.local.
# Generate a token at https://brightdata.com/cp/setting/users
# This demo uses the FREE tier (dataset triggers) — no Pro plan required.
BRIGHTDATA_API_KEY=
